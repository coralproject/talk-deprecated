{
    "type": "user",
    "inferences": [
      {
        "related_ID_field": "comment_id",
        "predicate": "authors",
        "direction": "in",
        "required": false
      },
      {
        "related_ID_field": "flag_id",
        "predicate": "flags",
        "direction": "in",
        "required": false
      }
    ]
  }
